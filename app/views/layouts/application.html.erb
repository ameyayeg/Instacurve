<!DOCTYPE html>
<html>
  <head>
   
    <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
    <link href="https://fonts.googleapis.com/css2?family=Karla:ital,wght@0,400;1,700&display=swap" rel="stylesheet">
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.0.0-beta3/css/all.min.css" integrity="sha512-Fo3rlrZj/k7ujTnHg4CGR2D7kSs0v4LLanw2qksYuRlEzO+tcaEPQogQ0KaoGN26/zrn20ImR1DfuLWnOo7aBA==" crossorigin="anonymous" referrerpolicy="no-referrer" />
    <title>Instacurve</title>
    <meta name="viewport" content="width=device-width,initial-scale=1">
    
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>

    <%= stylesheet_link_tag "application", "data-turbo-track": "reload" %>
    <%= javascript_importmap_tags %>
  </head>

  <body>
    <nav class="navbar flex">
            <%= link_to "Instacurve", images_path, class: 'navbar-brand' %>
            <ul class="flex">
              <% if user_signed_in? %>
                  <%= link_to current_user.full_name, profile_path(current_user.username), class: 'nav-item' %>
                  <%= link_to "Log Out", destroy_user_session_path, class: 'nav-item', method: :delete %>
                  <%= link_to "Upload", upload_path, class: 'btn btn-primary' %>
              <% else %>
                  <%= link_to "Sign In", new_user_session_path, class: 'nav-item' %>
                  <%= link_to "Create an Account", new_user_registration_path, class: 'btn btn-primary' %>
              <% end %>
            
            </ul>
  </nav>

    <div class="container">
      <%= yield %>
    </div>
  </body>
</html>
