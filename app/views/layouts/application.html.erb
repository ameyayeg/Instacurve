<!DOCTYPE html>
<html>
  <head>
    <title>Gallery</title>
    <meta name="viewport" content="width=device-width,initial-scale=1">
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>

    <%= stylesheet_link_tag "application", "data-turbo-track": "reload" %>
    <%= javascript_importmap_tags %>
  </head>

  <body>
    <nav class="navbar">
      <div class="flex">
        <%= link_to "Gallery", root_path, class: 'navbar-brand' %>
        <ul class="navbar-nav">
            <%= link_to "Home", root_path, class: 'nav-item' %>
            <%= link_to "Recent", root_path, class: 'nav-item' %>
            <%= link_to "Upload", upload_path, class: 'btn btn-primary' %>
        </ul>
        <ul class="navbar-nav">
            <% if user_signed_in? %>
                <%= link_to current_user.full_name, root_path, class: 'nav-item' %>
                <%= link_to "Log Out", destroy_user_session_path, class: 'nav-item', method: :delete %>
            <% else %>
                <%= link_to "Sign In", new_user_session_path, class: 'nav-item' %>
                <%= link_to "Create an Account", new_user_registration_path, class: 'btn btn-primary' %>
            <% end %>
        </ul>
      </div>
  </nav>

    <div class="container">
      <%= yield %>
    </div>
  </body>
</html>
